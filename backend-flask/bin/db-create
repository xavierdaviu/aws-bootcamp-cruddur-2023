#! /usr/bin/bash

#echo "db-create:"
CYAN='\033[1;36m'
NO_COLOR='\033[0m'
LABEL="db-create:"
printf "${CYAN}== ${LABEL}${NO_COLOR}\n"

CONNECTION_URL_NO_DB=$(sed 's/\/cruddur//g' <<<"$CONNECTION_URL")
psql $CONNECTION_URL_NO_DB -c "CREATE database cruddur;"